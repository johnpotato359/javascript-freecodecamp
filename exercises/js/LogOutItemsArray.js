let myCourses = ["Learn CSS Animations", "UI Design FUndamentals", "Intro to Clean Code"];

function logOutItems(array) {
    for (i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
}

logOutItems(myCourses);